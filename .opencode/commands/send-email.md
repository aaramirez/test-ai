---
description: Send email via SMTP with optional attachments.
---

Send an email using SMTP configuration from .env.

1. Verify SMTP credentials in .env (SMTP_HOST, SMTP_USER, SMTP_PASS)
2. Compose email with subject, body, and optional attachments
3. Send via send-email.js CLI or MCP server

Use `email` skill for SMTP setup and configuration details.
