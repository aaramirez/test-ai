---
description: Run the test suite for the current project.
---

Run the test suite. First detect the test framework (jest, vitest, pytest, rspec, etc.) by looking at package.json, pyproject.toml, Gemfile, or config files. Then run tests with appropriate flags (verbose, coverage if configured). Report results clearly — show which tests passed, failed, and any coverage summary.
