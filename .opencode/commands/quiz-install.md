---
description: Install the quiz and testing system to a directory. Usage: /quiz-install <target-dir>
---

Load the quiz-install skill. The user provided the target directory: $ARGUMENTS.
If no directory was provided, ask the user which directory to install to.
Follow the skill workflow: show a dry-run preview with --dry-run --verbose --dir, confirm, then install.
