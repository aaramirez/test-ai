---
description: Export vault content to professional PDF.
---

Export Obsidian vault content to professional PDF documents.

1. Load vault structure and branding configuration
2. Run `docgen-vault.js` with appropriate options
3. Verify output PDFs in generated/ directory
4. Apply brand colors, fonts, and logo

Use `vault-pdf-export` skill for export options and batch processing.
